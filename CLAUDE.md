# AIToolsWeb — 项目说明与协作规则

> 这个文件是项目的最高指导文件。**第一部分「协作规则」优先级最高，整个项目期内必须严格遵守。**

---

# 第一部分：协作规则

## 背景

项目所有者只熟悉 **Python** 一门语言，对前端、构建工具链、DevOps 不了解。

所以本项目的技术工作方式与常规不同：**用户不提供技术需求，只做技术决策。**

## 规则

### 1. 技术决策必须由用户拍板

任何涉及**技术选型、架构、目录结构、依赖引入、构建配置、部署方式**的事情，我不能直接做决定，必须：

1. 列出 **2–4 个候选方案**（不要只给一个让用户无从比较）
2. 每个方案说明四件事：
   - **是什么** —— 用 Python 能理解的概念类比
   - **优势**
   - **劣势**
   - **长期维护成本** —— 半年后还需不需要管它、会不会过时
3. 给出**明确推荐**并说明理由（不要中立罗列，把选择困难丢回给用户）
4. 用 `AskUserQuestion` 让用户选择，**等他确认后再动手**

### 2. 不要向用户索要技术细节

凡是"用什么框架""配置怎么写""依赖选哪个""目录怎么放"这类问题，**不要问用户**——他答不了，问了只会让他为难。

正确做法：自己查证（WebSearch / 查官方文档 / 看包的实际版本），然后列方案，或直接采用业界默认做法并说明理由。

### 3. 可以问用户的：产品层面和偏好层面

- 要什么功能、不要什么功能
- 界面长什么样、信息怎么排布
- 优先做哪个、先做哪部分
- 这个交互结果符不符合预期
- 文案、命名、配色这类主观偏好

### 4. 解释要用 Python 类比

用户能听懂：`dict`、`list`、`import`、虚拟环境、pip、装饰器、纯函数、模块。
用户听不懂：webpack、tree-shaking、SSR、虚拟 DOM、响应式、打包产物、chunk。

讲新概念时，先找 Python 里的对应物。例如：
- Vue 组件 ≈ 一个带 `render()` 的类，或者一个自带状态和输出的函数
- 组件 props ≈ 函数参数
- `computed` ≈ `@property`（带缓存的）
- `watch` ≈ 回调钩子
- Vite 打包 ≈ PyInstaller，把源码变成一个可发布的产物
- tree-shaking ≈ 只 import 用到的部分，没用到的代码不会进最终产物

### 5. 红线操作先确认

沿用全局规则，即使 auto-accept 模式也必须先问：

- `git commit`、`git push`、`git rebase`、`git reset --hard`
- 删除文件、目录或 git 历史
- 修改 `.env`、密钥、证书、**CI/CD 配置**
- 公开发布（`npm publish`、生产部署）

提交前先展示变更摘要。commit message 用简洁英文。

### 6. 沟通语言

中文回复。代码、命令、变量名、文件路径保持英文。

---

# 第二部分：产品文档

## 项目定位

**AIToolsWeb** —— 纯前端的开发者工具聚合站。

一句话：打开网页就能用的开发者小工具集合，**所有计算在浏览器本地完成，数据一个字都不上传**。

## 目标用户与场景

主要用户是开发者本人及同类人群，典型场景：

- 后端接口返回一坨压缩 JSON，要快速看清结构
- 拿到一个时间戳数字，不确定是秒还是毫秒，要转成人能读的时间
- 一段中文要 Base64，网上随便找的网站不敢粘（怕被记录）
- 下载的文件要核对 SHA-256 校验和
- 两段配置/日志要对比差异

这些场景的共同点：**一次性、随手、对隐私敏感**。所以设计取向是：打开即用、无需注册、不联网、不埋点。

## 功能规格（首批 6 个工具）

### 1. JSON 格式化

| 能力 | 说明 |
|---|---|
| 格式化 | 可选缩进 2 / 4 / Tab |
| 压缩 | 去掉所有非必要空白 |
| 树形视图 | 可折叠展开、语法着色、显示节点类型与数量 |
| 转义 / 去转义 | JSON 字符串与转义文本互转 |
| 错误定位 | 报错时给出行列号，并提供「跳到错误位置」按钮（自动聚焦并选中） |
| 键排序 | 可选按 key 字母序排列，便于对比两个 JSON |

### 2. 时间戳转换

| 能力 | 说明 |
|---|---|
| 实时当前时间戳 | 每秒刷新，秒和毫秒同时显示，一键复制 |
| 时间戳 → 时间 | 秒/毫秒显式切换（不靠位数猜），多时区并列显示 |
| 时间 → 时间戳 | 支持多种日期格式输入 |
| 多时区 | 本地 + UTC + 用户自选时区 |
| 多格式输出 | ISO 8601（本地偏移）、RFC 2822、人类可读、相对时间（3 小时前） |

### 3. Base64 编解码

| 能力 | 说明 |
|---|---|
| 文本编解码 | UTF-8 安全，中文/emoji 正确往返 |
| URL-safe 变体 | `+/` ↔ `-_`，去掉 `=` 填充 |
| 图片转 Base64 | 拖拽或选择图片，输出 Data URL，显示原始体积与膨胀后体积 |
| 二进制检测 | 解码结果不是合法 UTF-8 时明确提示，而不是显示一串乱码 |

### 4. URL 编解码

| 能力 | 说明 |
|---|---|
| `encodeURIComponent` 双向 | 编码单个参数值 |
| `encodeURI` 双向 | 编码整条 URL，保留结构字符 |
| Query 参数解析 | 解析成表格，保留重复键，每行可单独复制 |
| `+` 号语义开关 | 是否把 `+` 当空格（两种规范行为不同，明确标注） |
| 非法编码定位 | 指出第一个非法 `%XX` 的位置 |

### 5. 哈希计算

| 能力 | 说明 |
|---|---|
| 算法 | SHA-1 / SHA-256 / SHA-384 / SHA-512，一次全算并列展示 |
| 输入类型 | 文本 / 文件 |
| 换行归一化 | 可选把 `\r\n` 统一成 `\n`（Windows 文本常见坑），并提示检测到多少处 |
| 输出 | 小写十六进制，每个算法独立复制按钮 |

### 6. 文本对比

| 能力 | 说明 |
|---|---|
| 逐行 diff | 并排显示，行号对齐，增删改高亮 |
| 行内差异 | 修改行内部再高亮到词级 |
| 忽略选项 | 忽略大小写、忽略空白字符 |
| 统计 | 新增 / 删除 / 修改行数 |

## 非功能性要求

- **隐私**：零上传、零请求、零埋点。所有计算在浏览器完成，不引入任何第三方统计脚本
- **离线可用**：构建产物是纯静态文件，加载后断网也能用
- **响应式**：窄屏（< 768px）侧边栏收起为抽屉
- **可访问性**：键盘可操作、语义化标签、`aria` 属性、主题切换尊重系统偏好
- **性能**：单页体积控制在 200 KB gzip 以内，首屏无阻塞

## 信息架构

- 左侧固定侧边栏，按类别分组列出工具
- 右侧工作区，一个工具一屏，不用来回跳转
- 路由：`#/t/<tool-id>`，hash 模式
- 无首页 —— 打开直接进入第一个工具

## 架构说明

### 目录结构

```
src/
├─ tools/          # 工具目录：新增工具只动这一层
│  ├─ registry.js  # 【唯一注册表】工具清单
│  └─ <id>/        # 一个工具一个目录
├─ components/
│  ├─ layout/      # 布局：侧边栏、顶栏、主题按钮
│  └─ ui/          # 公共 UI 组件，工具间复用
├─ composables/    # 可复用的有状态逻辑
├─ utils/          # 纯函数工具
└─ styles/         # 设计令牌与全局样式
```

### 新增一个工具的步骤

1. 建目录 `src/tools/<id>/`，写 `<Id>Tool.vue`（默认导出）
2. 在 `src/tools/registry.js` 的数组里追加一个对象

完成。路由、侧边栏、页面标题、持久化 key 全部自动生效，不需要碰任何其他文件。

### 关键技术机制

| 机制 | 做法 | 为什么 |
|---|---|---|
| 路由 | `createWebHashHistory()` 不传参 | GitHub Pages 无 rewrite 能力，history 模式刷新会 404；hash 模式服务器永远只收到 `/`，且 base 自动推导，换域名不用改代码 |
| 部署路径 | `vite.config.js` 里 `base: './'` | 同时兼容 `user.github.io/AIToolsWeb/`、自定义域名、localhost；硬编码仓库名会在改仓库名时白屏 |
| 主题 | `<html data-theme>` + CSS 变量 + `<head>` 内联脚本 | 内联脚本必须在首次绘制前执行才能防闪白，因此必须是普通 `<script>` 且位于所有样式之前；内联脚本与 `useTheme.js` 共享同一个 localStorage key 和三态语义，改一处必须改另一处 |
| 状态持久化 | `useToolState(toolId, defaults)` | 输入自动存 localStorage，防抖 400ms，切换工具时补写一次 |
| 依赖控制 | 能用浏览器原生 API 绝不用库 | 见下方 |

### 关于依赖

**运行时依赖只有 3 个**：`vue`、`vue-router`、`diff`。

没有 TypeScript、没有 ESLint、没有 CSS 框架、没有 UI 组件库。这是刻意的选择：依赖越少，半年后需要维护的东西越少，过时的风险越小。

优先使用浏览器原生能力：

| 需求 | 用什么 | 不引什么 |
|---|---|---|
| SHA 哈希 | `crypto.subtle.digest()` | crypto-js、js-sha256 |
| 时区/日期格式化 | `Intl.DateTimeFormat`、`Intl.supportedValuesOf('timeZone')` | dayjs + timezone、luxon |
| UTF-8 ↔ 字节 | `TextEncoder` / `TextDecoder` | text-encoding polyfill |
| Query 解析 | `URLSearchParams` | qs、query-string |
| 剪贴板 | `navigator.clipboard` + `execCommand` 降级 | clipboard.js |
| 代码编辑 | 原生 `<textarea>` + 等宽字体 | CodeMirror、Monaco |

**唯一的例外是 `diff`（jsdiff）**：diff 是"看起来简单、写对了很难"的算法，朴素的 LCS 实现是 O(n×m) 空间复杂度，两段几千行的文本就能把浏览器卡死。生产可用需要 Myers 算法加各种优化。自己写不划算。

## Roadmap（暂不实现）

按用户实际使用频率排序，后续可加：

- 正则表达式测试器
- JWT 解析（只解码不验证，纯本地）
- 颜色转换（HEX / RGB / HSL）、对比度检查
- UUID / 随机字符串生成
- Cron 表达式解析
- Markdown 预览
- 进制转换
- 文本处理（大小写转换、去重、排序、统计）
- 二维码生成
- 图片压缩 / 格式转换

---

# 第三部分：开发与部署

## 本地开发

```bash
npm install     # 首次
npm run dev     # 开发服务器，改代码自动刷新
npm run build   # 构建产物到 dist/
npm run preview # 本地预览构建产物（部署前务必跑一次）
```

## 部署

推送到 `main` 分支，GitHub Actions 自动构建并发布。

**首次需要手动做两件事**：

1. GitHub 上建一个 **public** 空仓库（不要勾选 Add README / .gitignore）
2. 仓库 **Settings → Pages → Build and deployment → Source** 改成 **`GitHub Actions`**
   —— 这一步不做会报 `Get Pages site failed`，且工作流无法代劳（需要 PAT 权限）

## 常见坑

按"踩到概率 × 排查难度"排序：

1. **部署后白屏，控制台 404 指向没有仓库名的路径** → `base` 配置问题，本项目已用 `base: './'` 根除
2. **Windows 大小写陷阱** → Windows 不区分大小写，Linux 区分。导入语句的写法必须和实际文件名**逐字符一致**。文件加载不了时第一件事查大小写
3. **缓存** → GitHub Pages 对所有资源返回 `max-age=600` 且无法修改。push 完立刻打开可能还是旧版本，Ctrl+F5 或等 10 分钟。这是固有行为，不是 bug
4. **`crypto.subtle` 和 `navigator.clipboard` 只在安全上下文可用** → HTTPS 或 localhost。用 `--host` 从局域网 IP 访问时会是 `undefined`，代码里要做能力检测
5. **`localStorage` 在 Safari 无痕模式下直接抛异常** → 所有读写必须包 try/catch
6. **Actions 版本具有时效性** → 当前用 `checkout@v7` / `setup-node@v7` / `configure-pages@v6` / `upload-pages-artifact@v5` / `deploy-pages@v5`。网上大量教程还在用 `@v4`，那些跑在 Node 20 上，GitHub 已在 2026-09-16 移除 Node 20
7. **`.nojekyll` 本项目不需要** → 走 `upload-pages-artifact` 路线时 GitHub 不跑 Jekyll。若将来改回 branch 部署则必须加，且要同时设 `include-hidden-files: true`（v5 默认排除点开头文件）
